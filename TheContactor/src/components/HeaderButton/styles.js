import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    icon: {
        aspectRatio: 1,
        color: 'black'
    },
    photoButton: {
        flexGrow: 1,
        justifyContent: 'center',
        overflow: 'hidden',
        marginLeft: 10,
        marginRight: 10
    }
});
